1.	Настройка получения данных из КАФКА
2.	Создаем конфигурацию Задачи
3.	Указываем источник = КАФКА
4.	Значение топика = mes.acs-2-mes.transformed.technology-params-fok.ver1
5.	Подключаем алгоритм EXCH_COMMON_LOADER
6.	in_OFFSET = Kafka:META - offset
7.	in_JSON = Kafka:null - mes.acs-2-mes.transformed.charge-shihta-822305.ver1
8.	in_KEY = Kafka:META - key
9.	in_FLOWNAME = EXCH_301_TECH_PARAMS_OKATYSH


``` SQL
SELECT
	--*
    data ->> 'exch_messagetimestamp' as exch_messagetimestamp,
    data ->> 'exch_datetime_process' as datetime_process,
    data ->> 'EXCH_REPORT_TIME_STAMP' as report_time_stamp,
    data ->> 'exch_messages_id' as exch_messages_id,
    data ->> 'exch_messagetypeid' as exch_message_type_id, 
    data ->> 'exch_message_type_name' as exch_message_type_name,
    data ->> 'exch_id_agr' as exch_id_agr,
    data ->> 'exch_agr_name' as exch_agr_name,
    data ->> 'exch_id_param' as exch_id_param,
    data ->> 'exch_param_name' as exch_param_name,
    data ->> 'exch_value_num' as exch_value_num,
    data ->> 'exch_value_str' as exch_value_str,
    data ->> 'exch_accounting_shift' as exch_accounting_shift
FROM
    msa_object_model.EXCH_TECH_PARAMS_OKATYSH
```