Паттерны
!!! Правило номер 1 , контролировать типы данных в объектной модели, и JSON схеме
!!! Правило номер 2, проконтролировать типы данных в объектной модели и условиях запроса
!!! Правило 3, для типа данных timestamp, Платформа упадет в ошибку если увидит микросекунд
!!! Десериализация для интегро объекта через AI
!!! Паттерн. Если где-то в алгоритме вы добавили счетчик и вы далее по логике есть условие разделяющее логику, то скорее всего на каждой ветке вытекающего из условия надо будет добавить увеличение этого счетчика

Вводные:

Значение 
- топика	mes.acs-2-mes.transformed.charge-shihta-bunker-cpo.ver1
- in_FLOWNAME	EXCH_304_CHARGE_SHIHTA_BUNKER
- EXCH_FLOWTEXT	Сообщения о расходе материалов по весодозаторам
- Интегро объект	EXCH_304_CHARGE_SHIHTA_BUNKER
- Имя таблицы: exch_304_charge_shihta_bunker

- Задача выборки	EXCH_304_CHARGE_SHIHTA_BUNKER
- Задача RUN	EXCH_304_RUN_CHARGE_SHIHTA_BUNKER
- Задача TO BP	EXCH_304_CHARGE_SHIHTA_BUNKER_TO_BP
    - периодичность: /30 * * * ?
- Запрос EXCH_304_CHARGE_SHIHTA_BUNKER
- Запрос EXCH_304_CHARGE_SHIHTA_BUNKER_TO_BP
- Алгоритм парсинга	EXCH_304_CHARGE_SHIHTA_BUNKER
- Алгоритм перекладывания в бизнес EXCH_304_CHARGE_SHIHTA_BUNKER_TO_BP

# План работ
## 1.	Создание задачи для выборки данных
## 2.	Создание нового FLOW REF - ok
## 3.	Создание JSON схемы
В идеале просто выбираем топик из списка и структура сама подтягивается.
Затем просто перепроверяем типы данных
## 4.	Создание интегро объекта - ок
Делаем таблицу маппинга JSON и атрибутов в объекте
## 5.	Создание запроса для выборки «сырых» сообщений
## 6.	Создание алгоритма парсинга
Скопировал имеющийся от 301 потока.
Надо заменить: 
-	ссылку на схему, 
-	ссылку на Объект, 
-	ссылку на запрос
-	в блоке логирования указать новое имя потока
-	в блоке запроса указать вычисление для нового параметра
-	в блоке для получения ИД экземпляра указать новое имя (вызывает сложность всегда)
-	маппинг данных выполнить
## 7.	Создание задачи для париснга
## 8.	Добавление алгоритма парсинга в STARTER_LOCAL
## 9.	UNIT-TEST парсинга
- сделать тестовое сообщения номер=3
- Отправить сообщение из КАФКА
- Проверить что данные легли в очередь для обработки
- Выполнить задачу для парсинга
- Проверить что данные распарсились
## 10.	Создание запроса для выборки необработанных интегро экземпляров
## 11.	Создание алгоритма для перекладывания в бизнес-слой
## 12.	Создание задачи для перекладывания в бизнес-слой
## 13.	UNIT-TEST перекладывания в бизнес-слой

``` SQL
SELECT
    (data->>'exch_datetime')::timestamp as exch_datetime,
    (data->>'exch_stream_id')::integer as exch_stream_id,
    (data->>'exch_material_id')::integer as exch_material_id,
    (data->>'exch_messages_id')::integer as exch_messages_id,
    (data->>'exch_bunker_number')::integer as exch_bunker_number,
    (data->>'exch_material_name') as exch_material_name,
    (data->>'exch_weight_double')::double precision as exch_weight_double,
    (data->>'exch_source_shop_id')::integer as exch_source_shop_id,
    (data->>'exch_message_type_id')::integer as exch_message_type_id,
    (data->>'exch_messagetimestamp')::timestamp as exch_messagetimestamp,
    (data->>'exch_message_type_name') as exch_message_type_name
FROM
    msa_object_model.exch_304_charge_shihta_bunker

```

