# Основной фреймворк для фоновых задач с поддержкой Redis
celery[redis]>=5.3.0

# Клиент для взаимодействия с Redis (устанавливается автоматически с celery[redis], 
# но явное указание версии — хорошая практика)
redis>=5.0.0

# Веб-интерфейс для мониторинга задач Celery (опционально, но очень рекомендуется)
flower>=2.0.0

# ==============================================================================
# ПРИМЕЧАНИЕ ДЛЯ ПОЛЬЗОВАТЕЛЕЙ WINDOWS:
# Если вы запускаете Celery в чистой Windows (без WSL2 или Docker), 
# раскомментируйте строку ниже, чтобы использовать пул потоков eventlet:
# eventlet>=0.33.0
# ==============================================================================